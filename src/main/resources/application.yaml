cloud:
  aws:
    region:
      static: us-east-1
    stack:
      auto: false
    credentials:
      profile-name: dev

logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error